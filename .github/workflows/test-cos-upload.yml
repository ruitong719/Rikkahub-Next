name: Test COS Upload

on:
  workflow_dispatch:

jobs:
  test_cos_upload:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取代码
        uses: actions/checkout@v5

      - name: 创建测试文件
        run: |
          mkdir -p test-output
          echo "RikkaHub COS Test - $(date)" > test-output/test.txt
          echo "Build: ${GITHUB_SHA}" >> test-output/test.txt

      - name: 安装 Python COS SDK
        run: pip install cos-python-sdk-v5

      - name: 上传到 COS
        env:
          COS_SECRET_ID: ${{ secrets.COS_SECRET_ID }}
          COS_SECRET_KEY: ${{ secrets.COS_SECRET_KEY }}
          COS_BUCKET: ${{ secrets.COS_BUCKET }}
          COS_REGION: ap-guangzhou
        run: |
          python3 - << 'EOF'
          import os
          from qcloud_cos import CosConfig, CosS3Client

          secret_id = os.getenv("COS_SECRET_ID")
          secret_key = os.getenv("COS_SECRET_KEY")
          bucket = os.getenv("COS_BUCKET")
          region = os.getenv("COS_REGION", "ap-guangzhou")

          config = CosConfig(Region=region, SecretId=secret_id, SecretKey=secret_key, Scheme="https")
          client = CosS3Client(config)

          # 上传测试文件
          response = client.upload_file(
              Bucket=bucket,
              LocalFilePath="test-output/test.txt",
              Key=f"ci-test/{os.getenv('GITHUB_SHA')[:7]}.txt",
              PartSize=10,
              MAXThread=10,
              EnableMD5=False
          )
          print(f"✅ 上传成功！ETag: {response.get('ETag')}")

          # 验证
          head = client.head_object(Bucket=bucket, Key=f"ci-test/{os.getenv('GITHUB_SHA')[:7]}.txt")
          print(f"文件大小：{head.get('Content-Length')} 字节")
          EOF
